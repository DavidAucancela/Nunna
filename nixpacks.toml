[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm install -g pnpm@9", "pnpm install --frozen-lockfile"]

[phases.build]
cmds = ["pnpm build --filter @seres-del-pase/web"]

[start]
cmd = "pnpm --filter @seres-del-pase/web start"
